/** @type {import('tailwindcss').Config} */
export default {
  // Make sure to add your 'content' paths here if you haven't already!
  theme: {
    extend: {
      fontFamily: {
        aileron: ['Aileron', 'sans-serif'],
        work: ['"Work Sans"', 'sans-serif'],
      },
    },
  },
  plugins: [],
};